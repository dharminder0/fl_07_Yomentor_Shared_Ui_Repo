import { StyleSheet, Text, View } from "react-native";
import React from "react";

const EnrolledBatches = () => {
  return (
    <View>
      <Text>EnrolledBatches</Text>
    </View>
  );
};

export default EnrolledBatches;

const styles = StyleSheet.create({});
